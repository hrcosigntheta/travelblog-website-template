---
interface Props {
  title: string;
  description?: string;
  image?: string;
  canonicalURL?: URL | string;
  ogType?: 'website' | 'article' | 'profile';
}

const {
  title,
  description = 'A high-performance travel blog featuring immersive stories and guides.',
  image = '/images/og-default.jpg',
  canonicalURL = new URL(Astro.url.pathname, Astro.site),
  ogType = 'website',
} = Astro.props;

// Compute base path for assets
const base = import.meta.env.BASE_URL === '/' ? '' : import.meta.env.BASE_URL.replace(/\/$/, '');

// Ensure image is an absolute URL with base path
// If image is a relative path (starts with /), prepend the base path
const imageWithBase =
  image.startsWith('/') && !image.startsWith('//') && !image.startsWith('http')
    ? `${base}${image}`
    : image;
const socialImageURL = new URL(imageWithBase, Astro.site || Astro.url);
---

<meta charset="UTF-8" />
<meta name="description" content={description} />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<link rel="icon" type="image/svg+xml" href={`${base}/favicon.svg`} />
<link rel="icon" type="image/png" sizes="32x32" href={`${base}/favicon-32x32.png`} />
<link rel="icon" type="image/png" sizes="16x16" href={`${base}/favicon-16x16.png`} />
<link rel="apple-touch-icon" href={`${base}/apple-touch-icon.png`} />
<link rel="manifest" href={`${base}/manifest.json`} />
<meta name="theme-color" content="#0ea5e9" />
<meta name="generator" content={Astro.generator} />

<!-- Canonical URL -->
<link rel="canonical" href={canonicalURL} />

<!-- Open Graph / Facebook -->
<meta property="og:type" content={ogType} />
<meta property="og:url" content={Astro.url} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={socialImageURL} />

<!-- Twitter -->
<meta property="twitter:card" content="summary_large_image" />
<meta property="twitter:url" content={Astro.url} />
<meta property="twitter:title" content={title} />
<meta property="twitter:description" content={description} />
<meta property="twitter:image" content={socialImageURL} />

<title>{title}</title>
